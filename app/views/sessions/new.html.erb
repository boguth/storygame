<%= form_for :session, url: sessions_path, method: "post" do |f| %>
  <%= f.text_field :username, placeholder: 'Username', value: @attempted_username %>
  <%= f.password_field :password, placeholder: "Password" %>
  <%= f.submit "Login" %>
<% end %>
<% if @error %>
<%= @error %>
<% end %>
